# Public API endpoint (exposed to browser)
NEXT_PUBLIC_API_BASE=http://localhost:8000

# Server-side only (secure, not exposed to browser)
API_BASE=http://localhost:8000
ADMIN_TOKEN=sync my expenses please