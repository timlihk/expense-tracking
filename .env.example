# Server
APP_BASE_URL=https://your-railway-app.up.railway.app
ENVIRONMENT=production

# Database - Supabase PostgreSQL (free tier: 500MB)
# Get from: Supabase Dashboard → Settings → Database → Connection string
DATABASE_URL=postgresql+psycopg2://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres

# Zoho OAuth
ZOHO_CLIENT_ID=your_client_id
ZOHO_CLIENT_SECRET=your_client_secret
ZOHO_BASE=https://www.zohoapis.com
ZOHO_ACCOUNTS_BASE=https://accounts.zoho.com
ZOHO_SCOPES=ZohoExpense.expenses.READ,ZohoExpense.reports.READ,ZohoExpense.files.READ

# Crypto key (Fernet). Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=replace_me

# Scheduler
SYNC_INTERVAL_MINUTES=15

# Admin - Simple secret phrase for manual sync
ADMIN_TOKEN=sync my expenses please
